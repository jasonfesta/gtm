# Agent and Ops tests

These tests cover discovery, replies, CRM/PostHog handoffs and CLI routing. CRM and LinkedIn implementation tests stay alongside their runtime. Run `make source-check` from the repository root.

# LinkedIn schema

schema.sql defines the local channel ledger. It records account-bound history and recovery state; it is separate from the shared CRM. Use the documented store/setup operations rather than manually replacing an existing ledger.

Start with [setup](../../../docs/SETUP.md).

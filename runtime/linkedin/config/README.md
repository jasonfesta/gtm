# LinkedIn configuration examples

account.example.json is a portable template, not an active account. Generate the receiving operator’s actual configuration in the private accounts folder after verifying their LinkedIn identity. Never insert credentials into this example.

Start with [setup](../../../docs/SETUP.md).

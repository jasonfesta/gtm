# X configuration

`account.example.json` is a portable account-policy template. Save actual operator bindings privately, verify the signed-in identity and select the intended X Pro deck during the run. The [Human Reply runbook](../../../agents/human-reply-agent/README.md) defines public reply behavior.

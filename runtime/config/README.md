# Portable configuration

This folder contains credential-free examples and schemas. Actual account bindings and secrets belong in the private runtime/accounts folder. Copy only the example needed for a workflow, configure the intended operator and verify account identity before use. An example configuration does not activate a schedule or authorize sending.

Start from [daily workflows](../../agents/human-reply-agent/README.md) or [account setup](../../docs/ACCOUNTS.md).
